alias ll='ls -l'
alias la='ls -A'
alias l='ls -CF'

alias em=emacsclient

function mkcd() {
  mkdir $1 && eval cd $1
}

alias r=redmine

alias b='bundle exec'

alias git='LANG=en_US git'
alias g='git status'
alias gr='git gr'
